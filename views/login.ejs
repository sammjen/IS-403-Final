<!DOCTYPE html>
<html>
<head>
    <title>Login | Good News Network</title>
    <style>
        * {
            box-sizing: border-box;
        }

        body {
            font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Arial, sans-serif;
            margin: 0;
            padding: 0;
            background: #f5f7fb;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
        }

        .back-link {
            margin-bottom: 14px;
            font-size: 0.85rem;
        }

        .back-link a {
            color: #2563eb;
            text-decoration: none;
            font-weight: 500;
        }

        .back-link a:hover {
            text-decoration: underline;
        }

        .card {
            background: #ffffff;
            border-radius: 16px;
            padding: 24px 26px 20px;
            box-shadow: 0 10px 25px rgba(15, 23, 42, 0.12);
            width: 100%;
            max-width: 420px;
            border: 1px solid #e5e7eb;
        }

        h2 {
            margin-top: 0;
            margin-bottom: 4px;
            font-size: 1.4rem;
        }

        .subtitle {
            margin: 0 0 16px 0;
            font-size: 0.9rem;
            color: #6b7280;
        }

        form {
            margin-top: 8px;
        }

        label {
            display: block;
            margin-top: 10px;
            font-size: 0.85rem;
            font-weight: 500;
            color: #374151;
        }

        input {
            width: 100%;
            margin-top: 4px;
            padding: 8px 10px;
            border-radius: 8px;
            border: 1px solid #d1d5db;
            font-size: 0.9rem;
        }

        input:focus {
            outline: none;
            border-color: #2563eb;
            box-shadow: 0 0 0 1px rgba(37, 99, 235, 0.2);
        }

        .btn {
            margin-top: 16px;
            width: 100%;
            border: none;
            border-radius: 999px;
            padding: 10px 14px;
            font-size: 0.95rem;
            font-weight: 600;
            background: #2563eb;
            color: #ffffff;
            cursor: pointer;
        }

        .btn:hover {
            filter: brightness(0.95);
        }

        .error-message {
            color: #b91c1c;
            background: #fee2e2;
            border: 1px solid #fecaca;
            padding: 8px 10px;
            border-radius: 8px;
            margin-bottom: 10px;
            font-size: 0.85rem;
        }

        .footer {
            margin-top: 12px;
            font-size: 0.85rem;
            color: #6b7280;
            text-align: center;
        }

        .footer a {
            color: #2563eb;
            text-decoration: none;
            font-weight: 500;
        }

        .footer a:hover {
            text-decoration: underline;
        }

        .brand {
            font-size: 0.8rem;
            text-transform: uppercase;
            letter-spacing: 0.08em;
            color: #9ca3af;
            margin-bottom: 4px;
        }
    </style>
</head>
<body>

<div class="back-link">
    <a href="/feed">← Back to Feed</a>
</div>

<div class="card">
    <div class="brand">Good News Network</div>
    <h2>Welcome back</h2>
    <p class="subtitle">Log in to see what good things are happening.</p>

    <% if (error_message && error_message.length > 0) { %>
        <div class="error-message"><%= error_message %></div>
    <% } %>

    <form method="POST" action="/login">
        <label>Email
            <input type="email" name="email" required>
        </label>

        <label>Password
            <input type="password" name="password" required>
        </label>

        <button type="submit" class="btn">Login</button>
    </form>

    <div class="footer">
        No account?
        <a href="/register">Create one</a>
    </div>
</div>

</body>
</html>
